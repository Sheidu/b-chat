name: frontend

environment:
  sdk: '^3.2.0'

dependencies:
  flutter:
    sdk: flutter
  socket_io_client: ^3.1.4
  provider: ^6.1.2
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
